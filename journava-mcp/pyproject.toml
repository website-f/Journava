[project]
name = "journava-mcp"
version = "0.1.0"
description = "MCP server exposing Journava's autonomous travel agent mesh as tools"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.9.4",
    "httpx>=0.27",
]
